# Table

Styles `table`, `th`, and `td`. Headers get a subtle background and stronger
weight; cells get a light bottom border. There are no `--table-*` tokens — the
feature styles the native elements directly with the shared design tokens.

## Tokens

None. This feature has no component-specific tokens; see the global **Tokens**
story for the shared colour, spacing, and font-weight scales it uses.